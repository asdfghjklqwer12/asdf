# 소스와 테스트 이름에 한글이 들어간다. 빌드 JVM 이 ASCII 로케일이면
# 컴파일러가 한글 이름 클래스 파일을 못 써서 깨지므로, 실행 환경 로케일을 UTF-8 로 둘 것
# (예: LANG=C.UTF-8). file.encoding 만으로는 sun.jnu.encoding 이 안 바뀐다.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official

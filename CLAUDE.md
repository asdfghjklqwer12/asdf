# 이 저장소에서 일하는 방법

학습 streak 안드로이드 앱. 지금은 **규칙 엔진만** 있고 UI는 없다.
기획은 확정됐고(`기획 노트` 5장·6장), 규칙은 파이썬으로 먼저 검증한 뒤 Kotlin으로 옮겼다.

## 모듈

```
:domain   Android 의존성이 없는 순수 Kotlin. 규칙 전부가 여기 있다.
:sim      :domain 을 돌려보는 실행기 (시나리오 · 측정). :domain → :sim 방향은 없다.
:app      아직 없음. settings.gradle.kts 의 TODO 참고.
```

## 가장 중요한 규칙 — `:domain` 은 함부로 고치지 않는다

`:domain` 의 판정 로직은 **파이썬 구현이 정답**이라는 전제로 옮긴 것이고, 두 구현의 출력을
직접 맞대어 동일함을 확인했다(분배 19,100줄 · streak 37,604줄). 더 나은 방법이 보여도
바꾸지 마라. 이상해 보이는 부분이 있으면 고치지 말고 **사용자에게 물어봐라.**

특히 아래는 시뮬레이션으로 여러 번 고쳐서 나온 결론이라 건드리면 안 된다.

- △로든 ✕로든 초기화가 일어나면 최근 7일 창의 △를 모두 소진 처리한다
- △/✕는 과목 단위가 아니라 그날 필수 과목 **태스크 합산 비율 30%**로 가른다
- ✕를 전체 프리즈로 막은 날은 streak이 +1이 아니라 **그 자리에 멈춘다**
- 과목 프리즈로 지켜진 과목은 전체 판정에서 **완료로 치지 않는다**
- 프리즈를 보유 중이면 조각은 **5개가 상한**이다
- 그날 태스크가 0개인 과목은 휴식일이다

## 나중에 얹은 규칙 — 이월 (carry-over)

파이썬 원본에는 없고 사용자가 나중에 요청한 것이다. `Subject.carryOver` 로 **과목마다 켠다**.

**기본값이 꺼짐인 건 우연이 아니라 측정으로 정한 것이다** (`docs/측정-결과.md` 12절).
이월은 진도를 지키는 대신 streak을 희생하고, 3장 가설상 streak이 사용자를 붙잡는 장치다.

**켰다면 △가 생기는 즉시 사용자에게 물어봐야 한다** (`SettleResult.carriedOver` 가 신호).
즉시 물어보면 판정이 이월을 안 쓴 것과 완전히 같아진다. 안 물어보고 쌓으면 많이 빠지는
사용자의 ○ 가뭄이 42일까지 간다 — 두 달 가까이 성취감이 한 번도 없다는 뜻이다.

카드의 **선택지는 둘**이다 — [계획 조정] 과 [내일 같이 하기]. 하나로 줄이지 마라.
`CatchUpChoice` · `applyCatchUp` 이 그 둘을 받는다.
**재분배와 `clearCarryOver` 를 따로 부르지 마라** — 둘은 한 쌍이고, 따로 부르면 밀린 몫을
두 번 요구한다. `applyCatchUp` 이 그걸 막는 것이 존재 이유다.

- 못 한 태스크가 **다음 학습일**로 넘어간다. 휴식일은 건너뛴다
- 넘어간 만큼 다음 날 배정량이 늘고, **그걸 전부** 체크해야 그 과목이 완료다
- 이월분은 30% 판정의 **분모에도 들어간다**
- 과목 프리즈가 streak 을 지켜줘도 진도는 안 나갔으므로 이월은 쌓인다
- 사용자가 계획을 다시 짜면 `Subject.clearCarryOver()` 로 지운다.
  기간 플랜이면 `redistribute` 와 짝으로 쓴다 — 안 그러면 밀린 몫을 두 번 요구한다
- `maxDaysWorth` = **하루에 최대 며칠치까지** 나올 수 있나. 기본값 2 = "하루 최대 이틀치".
  사용자가 화면에서 보는 숫자 그대로다. 이월로 넘어오는 건 `(maxDaysWorth - 1)` 일치까지
- 제한을 풀면(`null`) 여력 없는 사용자에게 ✕가 96%까지 오른다 (`docs/측정-결과.md` 11절)
- **한도를 넘친 몫은 계획에서 사라진다.** 진도를 맞추는 건 재분배의 일이다.
  사라진 양은 `SettleResult.carryDropped` 로 알려준다 — 그때 재분배를 권하면 된다

**`settle` 안에서 `tasksOn` 을 두 번 부르지 마라.** 1단계가 `carriedTasks` 를 갱신하므로
그날 배정량은 `requiredToday` 로 **먼저 확정한다.** 안 그러면 방금 넘긴 몫을 오늘 분량으로
한 번 더 센다 (실제로 났던 버그다).

## 분량 계획과 체크리스트를 잇는 다리

`allocate()` 는 "9월 9일은 7페이지" 까지만 낸다. 화면에 그리고 판정하려면 **체크박스 줄**이
있어야 한다 — 30% 판정은 페이지가 아니라 **체크한 줄 수**로 한다.

```kotlin
val checklist = allocation.toChecklist()          // 하루치를 DEFAULT_TASKS_PER_DAY 줄로
val subject   = checklist.toSubject("정석", required = true)
```

- 나머지는 **앞줄이 갖는다** — 7페이지면 3p + 2p + 2p
- 분량이 줄 수보다 적으면 그만큼만 (1페이지짜리 날은 한 줄)
- 버퍼일(0페이지)은 줄이 없어 **휴식일**이 된다 → 자동 ✕가 안 찍힌다
- `toSubject` 는 `sameDay = true` 를 쓴다. 이름이 당일 계획처럼 보이지만 실제 뜻은
  **"날짜마다 태스크 수가 정해지는 과목"** 이고, 자동 분배 플랜도 그 구조가 맞다

`DEFAULT_TASKS_PER_DAY` 는 **3** 이다. 168만 플랜을 짝지어 재서 정했다
(`docs/측정-결과.md` 14·15절). **줄 수는 ○ 비율을 안 바꾼다** — 폭 0.0%p 로 똑같다.
줄 수가 정하는 건 **✕를 면하려면 몇 페이지를 읽어야 하나** 하나뿐이고, 30% 기준이
줄 단위로 올림되기 때문이다. 3줄이면 7p 날에 3p(43%), 6p 날에 2p(33%) 로
기획 5.2의 명목 기준 30% 에 가장 가깝다. 2줄이면 3페이지를 읽고도 ✕다.
6·7줄이 3줄과 동점이지만 화면만 복잡해진다. **바꾸지 마라.**

## 나중에 얹은 규칙 — 48시간 복구 (기획 5.5)

기획서엔 `[결정됨]` 인데 파이썬에 구현이 없어서 새로 짰다. 끊긴 날의 **밀린 하루치**를
다음 이틀 안에 다 하면 숫자가 돌아온다. 달력 월 1회.

```kotlin
val offer = engine.recoveryOffer(account, subjects, today)   // 카드를 띄울지
engine.recover(account, subjects, today, makeUp)             // 다 했으면 무른다
```

- **복구는 초기화만 무른다. 그날의 ○△✕ 는 안 바뀐다.** 그래서 △로 끊긴 날은 △의 +1이
  살아 있고, ✕로 끊긴 날은 +1 없이 그 자리에 멈춘다 — `FreezeDefended` 와 같은 모양이다.
  ○로 바꾸지 마라. 안 한 날을 한 것처럼 기록이 거짓이 된다 (`DayLog.recoveredAt` 도장만 찍는다)
- **끊기기 전 값을 더한다** (`overall += overallBefore`). 끊긴 뒤 오늘까지 올린 숫자가
  안 날아가게 하려면 대입이 아니라 덧셈이어야 한다
- **되돌릴 수 있는 건 가장 최근 끊김 하나다.** 창 안에 또 끊기면 그게 대상이 되고,
  `overallBefore` 가 0이면(0에서 0으로 끊긴 것) 제안하지 않는다
- **△ 3개로 끊긴 것도 대상이다.** ✕만 대상으로 하면 이 기능은 **1년에 0번** 켜진다 —
  streak을 죽이는 건 ✕가 아니라 △다 (`docs/측정-결과.md` 16절). 빼지 마라
- **"48시간"을 실제 시각이 아니라 날짜로 센다.** 실제 시각으로 재면 앱을 늦게 여는 사용자가
  이득을 본다 (사흘 뒤에 열면 그때 끊김이 확정되고 거기서 48시간이 시작된다)
- 과목 프리즈가 지킨 과목은 안 끊겼으므로 복구가 안 건드린다. 한 번 더 얹으면 없던 날이 생긴다
- 이월이 켜진 과목은 밀린 몫이 **이미 다음 날 배정에 들어가 있다.**
  `RecoveryOffer.Available.alreadyCarried` 가 그 목록이다 — 앱이 같은 일을 두 번 시키면 안 된다

## 코드 제약

- **`:domain` 에 Android 의존성을 넣지 마라.** Compose 도 `Context` 도 안 된다
- **`LocalDate.now()` 를 직접 부르지 마라.** `StreakEngine` 이 `Clock` 을 주입받는다.
  모든 시각 판정은 나중에 서버 시각으로 갈아끼워야 한다
- **정산은 멱등해야 한다.** 같은 날짜를 두 번 정산해도 streak이 두 번 오르면 안 된다
- 의존 방향은 단방향이다. `:app`·`:sim` → `:domain`, 반대는 없다

## 테스트

```
./gradlew :domain:test
```

132개가 전부 통과해야 한다. 하나라도 깨지면 규칙이 바뀐 것이다.

| 테스트 | 개수 | 성격 |
|---|---|---|
| `StreakEngineTest` | 22 | `streak_sim.py` 검증을 1:1 이식 — **건드리지 마라** |
| `PlanAllocatorTest` | 16 | `allocation.py` 검증을 1:1 이식 — **건드리지 마라** |
| `SettlementContractTest` | 8 | 멱등성 · `Clock` 주입 · 7일 창 밖 로그 불변식 |
| `StreakBranchTest` | 14 | 파이썬이 단언하지 않은 분기 + 30% 경계 |
| `PlanBranchTest` | 10 | 파이썬이 단언하지 않은 분기 + 연쇄 재분배 |
| `CarryOverTest` | 22 | 이월 규칙 (파이썬에 없던 새 규칙) |
| `CatchUpTest` | 5 | 밀린 날의 두 갈래 (계획 조정 / 내일 같이 하기) |
| `ChecklistTest` | 14 | 분량 계획 → 체크리스트 → 과목 다리 |
| `RecoveryTest` | 21 | 48시간 복구 (파이썬에 없던 새 규칙) |

앞의 38개는 파이썬 검증을 그대로 옮긴 것이다. 이름도 내용도 바꾸지 마라.
그다음 32개도 파이썬을 돌려 기대값을 뽑은 것이라 마찬가지다.
`CarryOverTest` 22개와 `RecoveryTest` 21개만 파이썬에 없던 새 규칙이다.

**재분배는 반드시 원본 `Allocation` 에서 누적 완료량으로 다시 계산한다.**
직전 결과 위에 또 하면 분량이 부풀려진다 (`redistribute` KDoc 참고).

## 규칙을 눈으로 보기

```
./gradlew :sim:run                     60일 시나리오
./gradlew :sim:run --args="measure"    기획 노트 5.2 숫자 재측정
./gradlew :sim:run --args="rates"      완료율에 따른 ○ / △ / ✕ 분포
./gradlew :sim:run --args="skips"      빼먹는 날이 섞였을 때 ✕ 빈도
./gradlew :sim:run --args="tasks"      하루치 분할 (가상 모델 — 14절이 뒤집음)
./gradlew :sim:run --args="checklist"  같은 질문을 진짜 체크리스트로
./gradlew :sim:run --args="checklist-confirm"  그 답을 짝지은 비교로 확인
./gradlew :sim:run --args="recovery"   48시간 복구가 실제로 얼마나 켜지나
./gradlew :sim:run --args="buffer"     버퍼 비율 튜닝
./gradlew :sim:run --args="freeze"     프리즈가 실제로 막아주는 비율
./gradlew :sim:run --args="carry"      이월이 만드는 눈덩이
./gradlew :sim:run --args="churn"      이탈 위험으로 본 이월 설정
./gradlew :sim:run --args="replan"     "계획 조정" 을 자주 고르면 드는 값
./gradlew :sim:run --args="censoring"  측정이 시행 길이에 흔들리는지
```

측정에서 나온 것들은 `docs/측정-결과.md` 에 정리돼 있다.

**시뮬레이션에 난수 소비를 추가하지 마라.** 난수를 하루에 하나만 더 뽑아도 난수열이
통째로 밀려 문서의 수치가 재현되지 않는다. 안 쓰는 경로에서는 뽑지 마라
(`skipRate > 0.0 && rng.nextDouble() < skipRate` 처럼). 실제로 한 번 겪었다.

## 빌드 환경

- JDK 17 이상. 바이트코드는 17로 맞춘다 (Android 모듈과 맞추기 위해)
- 소스와 테스트 이름에 한글이 들어간다. **빌드 JVM 로케일이 UTF-8이어야 한다**
  (`LANG=C.UTF-8` 등). ASCII 로케일이면 컴파일러가 한글 이름 클래스 파일을 못 써서 깨진다
- 한글 이름 테스트 안에서 `assertThrows` 같은 **inline 함수를 직접 부르지 마라.**
  호출한 함수 이름으로 클래스 파일이 하나 생겨서 같은 문제가 난다.
  ASCII 이름 도우미로 한 겹 감싸라 (`PlanAllocatorTest.planErrorFrom` 참고)

## 이번 범위가 아닌 것

Room, Firebase, 네트워크, 알림. 사용자가 따로 요청하기 전에는 손대지 마라.
